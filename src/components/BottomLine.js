import React from 'react';
import {View} from 'react-native';

const BottomLine = () => <View style={{width: '100%', height: 1, backgroundColor: 'rgba(144, 160, 175, 0.15)'}} />;

export default BottomLine;
